import { TopLevelDepOptions } from './types';
declare type HexVersion = undefined | {
    title: string;
    labels: Record<string, string>;
};
export declare function getNonHexVersion(options?: TopLevelDepOptions): HexVersion;
export {};
