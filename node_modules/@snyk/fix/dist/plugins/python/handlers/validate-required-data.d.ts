import { EntityToFix, RemediationChanges, Workspace } from '../../../types';
export declare function validateRequiredData(entity: EntityToFix): {
    remediation: RemediationChanges;
    targetFile: string;
    workspace: Workspace;
};
