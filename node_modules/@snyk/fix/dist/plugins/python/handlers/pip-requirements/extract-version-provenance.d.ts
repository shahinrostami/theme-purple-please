import { ParsedRequirements } from './update-dependencies/requirements-file-parser';
import { Workspace } from '../../../../types';
export interface PythonProvenance {
    [fileName: string]: ParsedRequirements;
}
export declare function extractProvenance(workspace: Workspace, dir: string, fileName: string, provenance?: PythonProvenance): Promise<PythonProvenance>;
