import { EntityToFix, FixOptions } from '../../../../types';
import { PluginFixResponse } from '../../../types';
export declare function poetry(fixable: EntityToFix[], options: FixOptions): Promise<PluginFixResponse>;
