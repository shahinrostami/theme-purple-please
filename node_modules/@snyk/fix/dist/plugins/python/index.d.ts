import { EntityToFix, FixOptions } from '../../types';
import { FixHandlerResultByPlugin } from '../types';
export declare function pythonFix(entities: EntityToFix[], options: FixOptions): Promise<FixHandlerResultByPlugin>;
