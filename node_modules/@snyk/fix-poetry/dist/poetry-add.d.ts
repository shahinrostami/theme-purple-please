import Bottleneck from 'bottleneck';
import { ExecuteResponse } from '@snyk/child-process';
interface PoetryConfig {
    dev?: boolean;
    python?: string;
}
export declare const poetryAdd: ((arg1: string, arg2: string[], arg3: PoetryConfig) => Promise<ExecuteResponse>) & {
    withOptions: (options: Bottleneck.JobOptions, arg1: string, arg2: string[], arg3: PoetryConfig) => Promise<ExecuteResponse>;
};
export {};
