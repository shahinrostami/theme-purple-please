/// <reference types="node" />
import { Readable } from "stream";
import { ExtractAction, FileNameAndContent } from "./types";
export declare function applyCallbacks(matchedActions: ExtractAction[], fileContentStream: Readable, streamSize?: number): Promise<FileNameAndContent>;
export declare function isResultEmpty(result: FileNameAndContent): boolean;
