import { AppDepsScanResultWithoutTarget, FilePathToContent } from "./types";
export declare function jarFilesToScannedProjects(filePathToContent: FilePathToContent, targetImage: string): Promise<AppDepsScanResultWithoutTarget[]>;
