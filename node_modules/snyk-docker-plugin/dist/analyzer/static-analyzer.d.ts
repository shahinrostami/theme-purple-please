import { DockerFileAnalysis } from "../dockerfile";
import { ImageType } from "../types";
import { StaticAnalysis } from "./types";
export declare function analyze(targetImage: string, dockerfileAnalysis: DockerFileAnalysis | undefined, imageType: ImageType, imagePath: string, globsToFind: {
    include: string[];
    exclude: string[];
}, appScan: boolean): Promise<StaticAnalysis>;
