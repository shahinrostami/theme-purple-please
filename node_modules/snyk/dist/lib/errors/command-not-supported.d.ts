import { CustomError } from './custom-error';
export declare class CommandNotSupportedError extends CustomError {
    readonly command: string;
    readonly org?: string;
    constructor(command: string, org?: string);
}
