export interface DependencyUpdates {
    [from: string]: {
        upgradeTo: string;
    };
}
export interface ManifestFiles {
    [name: string]: string;
}
export declare type PackageManagers = 'pip' | 'setuptools' | 'pipenv' | 'poetry';
export declare const FILENAMES: {
    [key in PackageManagers]: {
        manifest: string;
        lockfile?: string;
    };
};
